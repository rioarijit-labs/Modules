{
  "$schema": "../../../schema/module.schema.json",
  "id": "bicep:res/network/private-dns-zone",
  "tool": "bicep",
  "name": "Private DNS zone",
  "summary": "Private DNS zone linked to virtual networks so private endpoint names resolve to private IPs.",
  "useWhen": [
    "You use private endpoints and need name resolution inside your networks",
    "You need one shared privatelink zone for a hub and its spokes"
  ],
  "avoidWhen": [
    "You already have central private DNS zones in a connectivity subscription: reference their resource IDs instead of creating duplicates"
  ],
  "tags": ["networking", "dns", "private-networking"],
  "wraps": { "source": "br/public:avm/res/network/private-dns-zone", "version": "0.8.1" },
  "pairsWith": ["bicep:res/network/virtual-network", "bicep:res/network/private-endpoint"],
  "maturity": "preview"
}
